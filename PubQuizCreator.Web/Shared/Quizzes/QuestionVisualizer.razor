@if (Question != null)
{
    <div class="small fw-semibold">
        @Question?.Text
        <MediaIcon MediaType="Question?.MediaType" />
    </div>
    <div class="small text-muted">→ @Question?.Answer</div>
}