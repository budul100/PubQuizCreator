<div class="unidirectional-input-wrapper">
    @if (Multiline)
    {
        <textarea
            class="@CssClass"
            placeholder="@Placeholder"
            rows="@Rows"
            @ref="inputRef"
            @oninput="HandleInput"
            @onblur="HandleBlur"
            @onkeydown="HandleKeyDown">
            @localValue
        </textarea>
    }
    else
    {
        <input 
            class="@CssClass"
            placeholder="@Placeholder"
            value="@localValue"
            @ref="inputRef"
            @oninput="HandleInput"
            @onblur="HandleBlur"
            @onkeydown="HandleKeyDown" />
    }

    @if (ShowClearButton && !string.IsNullOrEmpty(localValue))
    {
        <button 
            class="unidirectional-input-clear-btn"
            tabindex="-1" 
            type="button"
            @onclick="ClearAndNotifyAsync">
            &times;
        </button>
    }
</div>

